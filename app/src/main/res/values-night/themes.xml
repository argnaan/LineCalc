<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EsameSoftwareTLC" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:windowBackground">@color/backDark</item>
    </style>
    <style name="button_main">
        <item name="android:backgroundTint">@color/buttonColor_dark</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/buttonFont_dark</item>
        <item name="android:layout_margin">6dp</item>
    </style>

    <style name="buttonSettings">
        <item name="android:backgroundTint">#01584A</item>
    </style>
    <style name="button_calc">
        <item name="android:backgroundTint">@color/buttonColor_dark</item>
        <item name="android:textColor">@color/buttonFont_dark</item>
    </style>
</resources>